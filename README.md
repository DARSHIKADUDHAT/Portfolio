# Darshika Dudhat — Portfolio (Vite + React)

This is a minimal portfolio site focused on functionality and data rendering.

Setup

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Build

```bash
npm run build
npm run preview
```
